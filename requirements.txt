openai>=1.30.0
anthropic>=0.40.0
pillow>=10.3.0
pyyaml>=6.0
httpx>=0.27.0
python-slugify>=8.0
requests>=2.31.0           # TikTok Content Posting API calls + OAuth helper
instagrapi>=2.1.0          # (optional) Instagram publishing via the UNOFFICIAL API; also needs ffmpeg on PATH
